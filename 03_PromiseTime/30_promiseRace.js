/**
`Promise.race()` 是在 JavaScript 中一個非常實用的 Promise 方法，它可以讓我們同時執行多個非同步操作，並傳回最先完成的操作的結果或錯誤原因。請手寫實作 `Promise.race()` 方法。

備註：請思考當傳入的 iterable 為空時，回傳的 Promise 應如何表現（與原生 `Promise.race` 行為一致為佳）。
 */

function promiseRace(promises) {}
