/**
JavaScript 雖然內建了 `setTimeout` 控制程式暫停與繼續的方法，但使用起來有些繁瑣。請設計一個更流暢的機制，類似其他語言如 Java 和 Python 中的 `sleep` 函式，同時保持 JavaScript 非同步的特性。

可以搭配 `await sleep(ms)` 使用，或 `sleep(ms).then(...)` 在延遲後繼續執行。
 */

function sleep(duration) {}
