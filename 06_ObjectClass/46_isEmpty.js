/**
給定一個物件或陣列，請判斷它是否為空。以下為「空」的定義：

- 一個空物件不包含任何鍵值對。
- 一個空陣列不包含任何元素。

可以假設物件或陣列是透過 `JSON.parse` 解析得到的。

範例 1：
輸入： obj = { x: 5, y: 42 }
輸出： false
解釋：這個物件有兩個鍵值對，所以不為空。

範例 2：
輸入： obj = {}
輸出： true
解釋：這個物件沒有任何鍵值對，所以為空。
 */

var isEmpty = function (obj) {};
