/**
請實作一個函式，該函式會接收另一個函式做為參數 `fn`，並且接收一個以毫秒為單位的時間 `t`，並回傳該函式的防抖 (debounce) 後版本。防抖是指，在函式執行被延遲了 `t` 毫秒內，如果該函式再次被呼叫，原本的執行將會被取消。

例如，假設 `t = 50ms`，函式分別在 `30ms`、`60ms` 和 `100ms` 時呼叫。前兩個函式呼叫將被取消，第三個函式呼叫將在 `150ms` 執行。如果改為 `t = 35ms`，則第一個呼叫將被取消，第二個呼叫將在 `95ms` 執行，第三個呼叫將在 `135ms` 執行。

請在不使用 lodash 的 `_.debounce()` 函式的前提下完成實作。

概念範例：以 `debounce(log, 50)` 包裝後，若在 50ms 內連續觸發，只有最後一次會在延遲結束後執行；較早的排程會被取消。
 */

function debounce(fn, t) {}
