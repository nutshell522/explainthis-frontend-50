/**
實作一個 `argumentsLength` 函式，它會回傳有多少個參數被傳入。

範例一
輸入： args = [5]
輸出： 1
解釋：argumentsLength(5) 只傳遞了一個參數，因此應返回 1。

範例二
輸入： args = [{}, null, '3']
輸出： 3
解釋：argumentsLength({}, null, '3') 傳遞了三個值，因此應傳回 3。
 */

var argumentsLength = function (...args) {};
